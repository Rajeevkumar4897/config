server:
  port: 8089
spring:
  h2:
    console:
      enabled: true

  application:
    name: SELLER-SERVICE    

      
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8081/eureka/

  instance:
    hostname: localhost
